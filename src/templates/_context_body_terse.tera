{%- if use_rules | length > 0 -%}
## Do

{% for r in use_rules -%}
- **[{{ r.severity }}] {{ r.id }}** — {{ r.short_desc }} ([source]({{ r.source_url }}))
{% endfor %}
{%- endif -%}
{%- if avoid_rules | length > 0 -%}

## Don't

{% for r in avoid_rules -%}
- **[{{ r.severity }}] {{ r.id }}** — {{ r.short_desc }} ([source]({{ r.source_url }}))
{% endfor %}
{%- endif -%}
{%- if guidance | default(value=[]) | length > 0 -%}

## Taste guidance

{% for g in guidance -%}
- **{{ g.title }}**{% if g.text %} — {{ g.text | truncate(length=100) }}{% endif %}
{% endfor %}
{%- endif -%}
